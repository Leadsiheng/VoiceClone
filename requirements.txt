# VoiceClone Dependencies
# Install with: pip install -r requirements.txt
#
# Prerequisites (IndexTTS2):
#   git clone https://github.com/index-tts/index-tts.git ../index-tts
#   cd ../index-tts && uv sync --all-extras
#   (IndexTTS2 manages its own dependencies via uv, not pip)

# ASR
funasr>=1.0.0
torch-complex

# LLM APIs
openai>=1.0.0
requests>=2.28.0

# Web UI
gradio>=4.0.0

# Audio
soundfile>=0.12.0
numpy>=1.24.0

# Config
pyyaml>=6.0

# Subprocess management
psutil>=5.9.0
